// SalesChart.js
import React from 'react';

const SalesChart = ({ salesData }) => {
    // La logique pour afficher le graphique des ventes ira ici

    return (
        <div>
            {/* Affichage du graphique des ventes */}
        </div>
    );
};

export default SalesChart;
